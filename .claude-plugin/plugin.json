{
  "name": "jarvis-plugin",
  "version": "0.7.3",
  "description": "Persistent AI memory for Claude Code — remembers who you are, how you think, and what you've decided across every session",
  "author": {
    "name": "parasite2060"
  },
  "homepage": "https://github.com/parasite2060/jarvis-claude-plugin",
  "repository": "https://github.com/parasite2060/jarvis-claude-plugin",
  "license": "MIT",
  "userConfig": {
    "serverUrl": {
      "title": "Server URL",
      "type": "string",
      "default": "http://localhost:8000",
      "description": "Jarvis server API base URL"
    },
    "apiKey": {
      "title": "API Key",
      "type": "string",
      "sensitive": true,
      "description": "Bearer token for Jarvis server authentication"
    },
    "cacheDir": {
      "title": "Cache Directory",
      "type": "string",
      "default": "~/.jarvis-cache/ai-memory",
      "description": "Local file cache directory for vault files"
    },
    "workerDir": {
      "title": "Worker Workspace Directory",
      "type": "string",
      "default": "~/.jarvis-cache/worker",
      "description": "Local workspace for worker-owned state: logs, conversation queue, PID file. Separate from cacheDir which holds the synced vault."
    },
    "workerPort": {
      "title": "Worker Port",
      "type": "number",
      "default": 37777,
      "description": "Port for local background file sync worker"
    },
    "extraHeaders": {
      "title": "Extra Headers",
      "type": "string",
      "sensitive": true,
      "default": "",
      "description": "Additional HTTP headers as JSON object, e.g. {\"CF-Access-Client-Id\":\"...\",\"CF-Access-Client-Secret\":\"...\"}"
    },
    "fetchTimeoutMs": {
      "title": "Fetch Timeout (ms)",
      "type": "number",
      "default": 180000,
      "description": "Per-request timeout for the worker's POST /conversations and GET /conversations/position calls. Raise on slow uplinks or for very large transcripts; lower to fail fast in test setups."
    }
  }
}
